# Core
Django==5.2.*          # Current Django LTS (supported through Apr 2028)
djangorestframework==3.18.*
djangorestframework-simplejwt==5.5.*
django-environ==0.14.*
django-cors-headers==4.9.*

# Realtime / websockets
channels==4.3.*
channels-redis==4.3.*

# Async tasks
celery==5.6.*
redis==8.1.*

# Cache framework (Part P-014) — django-redis on a Redis DB index
# distinct from Celery's broker (see config/settings/base.py)
django-redis==7.0.*

# Database driver
psycopg2-binary==2.9.*

# International phone number validation (Part P-027) — Google's
# libphonenumber Python port, used to validate BusinessProfile.phone_number
# against the number's own country code (never a single hardcoded
# country's format, per architecture assumption A3).
phonenumbers==9.*

# Object storage (Part P-013) — S3-compatible, provider-agnostic
django-storages==1.14.*
boto3==1.43.*
python-magic==0.4.*

# Ops / observability
sentry-sdk==2.69.*
python-json-logger==4.1.*  # Part P-015: structured JSON request loggingو

pytest==8.*
pytest-django==4.*