"""
Permission classes module (Part P-011 skeleton).

This file intentionally contains no permission classes yet. It exists
only to establish the module location (`core.permissions`) so that
future parts can import from here without a path change later.

The real permission-flag system (business-owner-only actions, admin
moderation permissions, etc.) is built in Part P-019.
"""